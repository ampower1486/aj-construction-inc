# Playwright is a local QA tool only — scripts/verify.mjs drives the system
# Chrome install via channel:'chrome', so the bundled browsers are never used.
# Skipping the download keeps CI and Vercel installs fast (saves ~300MB).
playwright_skip_browser_download=1
